---
title: Mutex
---

A **mutex** (mutual exclusion lock) is a synchronization primitive that prevents multiple threads from simultaneously accessing a shared resource.

Only one thread can hold the mutex at a time. Others block until it is released.
